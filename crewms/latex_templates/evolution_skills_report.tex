\subsubsection{ \VAR{ evolution } }

\begin{multicols}{2}

    \raggedcolumns


    \begin{description}
    %{ if general_skills|length is gt 0 }%
    	\item [General Skills] \hfill
        \begin{itemize}
        %{ for skill in general_skills }%
            \item \VAR{ skill.name } \sc{\VAR{ skill.category }, rank \VAR{ skill.level|jc_rank }}
        %{ endfor }%
        \end{itemize}
    %{ endif }%
    %{ for task in tasks }%
    	\phantomsection\addcontentsline{toc}{paragraph}{\VAR{ task.name }}
    	\needspace{2\baselineskip}
        \item [\VAR{ task.name }] \sc{\VAR{ task.rank|jc_rank }}
        \begin{itemize}
        %{ for skill in task.skills }%
            %{ if skill not in general_skills }%
	            \item \VAR{ skill.name } \sc{\VAR{ skill.category }, rank \VAR{ skill.level|jc_rank }}
	        %{ endif }%
        %{ else }%
            \item \emph{No skills assigned}
        %{ endfor }%
        \end{itemize}
    %{ else }%
        \item [] \emph{No tasks assigned}
    %{ endfor }%
    \end{description}
\end{multicols}
